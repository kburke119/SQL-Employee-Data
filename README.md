# sql-challenge

## Background
Designed the tables to hold the data from all of the csv files that contain employee information. Then imported the CSV files into a SQL database in order to perform an analysis.

